import { Box } from '@mui/material';
import styled from 'styled-components';

export const ArrayContainer = styled(Box)`
  display: flex;
  flex-direction: column;
  gap: 1rem;
`;

export const ArrayItem = styled(Box)`
  display: flex;
  gap: 1rem;
`;
