import { FC } from 'react';

import * as Styled from './recipes-tab.styled';

const RecipesTab: FC = () => {
  return <Styled.Container> RecipesTab </Styled.Container>;
};

export default RecipesTab;
